#include<stdio.h>

int main()
{
  int a, fact;
  printf("Enter the number for its factorial: ");
  scanf("%d", &a);
  fact=1;
  for (int i=1; i<=a; i++ )
  {
    fact=fact*i;
  };
  printf("The Factorial of the number is %d ", fact);
  return (0);
}
